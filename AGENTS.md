# Agent Notes

Keep this repo as one Tampermonkey userscript, not a Chrome extension.

Rules:

- Do not use Wave or AliExpress APIs.
- Do not commit personal account names, credit card digits, private vendors, business categories, order history, or local debug endpoints.
- Put user-specific defaults in `GM_setValue`/`GM_getValue` through `src/core/settings.js`.
- Keep feature modules small and expose one public `ensure()` entrypoint.
- Update the `sourceFiles` list in `tools/build-release.js` when adding or removing source modules. This list controls both the release concatenation order and the dev loader manifest.
- Keep `userscript/accounting-helpers.dev.user.js` generated by `tools/build-release.js`; do not hand-edit its module list.
- Run `node tools/build-release.js` after source-module, metadata, settings, UI, or behavior changes so `dist/accounting-helpers.user.js`, `userscript/accounting-helpers.release.user.js`, and `userscript/accounting-helpers.dev.user.js` stay aligned.
- For local Tampermonkey testing, run `node tools/dev-server.js` and use the installed dev userscript. It loads current repo files from the local dev server on each page load.
- Bump the userscript version in `tools/build-release.js` and `src/core/constants.js` for every user-facing behavior, settings, or UI change.
- Run `node tools/build-release.js` before testing the installable release script.
